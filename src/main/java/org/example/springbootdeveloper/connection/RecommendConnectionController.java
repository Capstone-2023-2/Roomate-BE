package org.example.springbootdeveloper.connection;

import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.Map;

@RestController
public class RecommendConnectionController {
    /*
    private final WebClientServiceImpl webClientServiceImpl;

    public RecommendConnectionController(WebClientServiceImpl webClientServiceImpl) {
        this.webClientServiceImpl = webClientServiceImpl;
    }

    @PostMapping("/api/card")
    public ResponseEntity<Map<String, Object>> test() {
        Map<String, Object> response = webClientServiceImpl.postUpdate();
        return ResponseEntity.ok(response);
    }

     */
}

